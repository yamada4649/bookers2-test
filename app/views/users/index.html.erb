<%= render 'users/new', users: @users %>

    <h1>Users</h1>

        <table border-bottom>
            <tr>
                <th>image</th>
                <th>name</th>
                <th></th>
            </tr>
            <% @users.each do |user| %>
                <tr>
                    <td><%= user.profile_image %></td>
                    <td><%= user.name %></td>
                    <td><%= link_to "Show", user_path(params[:id]) %></td>
                </tr>
            <% end %>
        </table>




		</div>
	</div>
</div>
